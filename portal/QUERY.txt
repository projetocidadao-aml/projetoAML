SELECT DISTINCT p.ID_POSTAGEM,hd.HISTORICO_ID, p.CONTEUDO, date_format(hd.HISTORICO_DATA, '%d/%m/%Y') as DATA_HISTORICO FROM POSTAGEM AS p
INNER JOIN HISTORICO_DESAFIO AS hd ON p.ID_POSTAGEM = hd.POSTAGEM_ID
;

-- PEGA O HISTÓRICO ID AGORA E JOGA NA OUTRA QUERY PARA PODER PEGAR AS MIDIAS 



SELECT ps.MIDIA_URL_TEXT, ps.MIDIA_TIPO_ARQUIVO  FROM POST_MIDIA AS ps WHERE ps.HISTORICO_ID = COLOCAR AQUI O RESULTADO DA PRIMEIRA QUERY